import React from 'react';
import { ScrollView, Text } from 'react-native'

const Layout = () => {
  return(
    <ScrollView>
      <Text>Layout view text</Text>
    </ScrollView>
  )
}

export default Layout